@model Website
@{
    if (Model.Id == 0)
    {
        ViewBag.Title = "Mailling List Template for All Website";
    }
    else
    {
        ViewBag.Title = "Mailling List Template for " + Model.Name;
    }
}
<div class="head">
    <h3>
        @ViewBag.Title</h3>
    <div>
        <a type="button" class="btn btn-primary" href="@Url.Action("Add")">New Template</a>
    </div>
</div>
<div class="mws-panel grid_8">
    <div class="mws-panel-header">
        <span><i class="icon-table"></i>@ViewBag.Title</span>
    </div>
    <div class="mws-panel-body no-padding listuser">
        @Html.Action("List")
    </div>
</div>
